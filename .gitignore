# See https://help.github.com/articles/ignoring-files for more about ignoring files.
#
# Temporary files generated by your text editor or operating system
# belong in git's global ignore instead:
# `$XDG_CONFIG_HOME/git/ignore` or `~/.config/git/ignore`

# Ignore bundler config.
/.bundle

# Ignore all environment files.
/.env*

# Ignore all logfiles and tempfiles.
/log/*
/tmp/*
!/log/.keep
!/tmp/.keep

# Ignore pidfiles, but keep the directory.
/tmp/pids/*
!/tmp/pids/
!/tmp/pids/.keep

# Ignore storage (uploaded files in development and any SQLite databases).
/storage/*
!/storage/.keep
/tmp/storage/*
!/tmp/storage/
!/tmp/storage/.keep

/public/assets

# Ignore master key for decrypting credentials and more.
/config/master.key

/app/assets/builds/*
!/app/assets/builds/.keep

config/application.yml
node_modules

# normally I'd keep this but tailwind 4 is reading the vendor asset files and it's causing issues.
# Also it's a lot
# see https://tailwindcss.com/docs/v4-beta#automatic-source-detection for adding a source back
# /vendor/javascript/*

# this showed up on my linux machine, no idea what it is. something like a docker log
y
/config/credentials/development.key

/config/credentials/production.key
